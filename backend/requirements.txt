# Runtime dependencies. Versions pinned from a live PyPI snapshot taken
# 2026-08-22 (see ADR-004) — bump deliberately, not by accident.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3

# Agent graph (wired up starting Day 2)
langgraph==1.2.11
langchain-google-genai==4.3.5
langchain-openai==1.6.0
openai==3.3.1

# Database / memory / trace log / approval checkpointing (Day 3, ADR-014).
# No ORM and no supabase-py client (a REST/Storage/Auth SDK, not a Postgres
# driver, and unused here — see ADR-014's correction of ADR-004): raw SQL
# over the same DATABASE_URL connection string via psycopg.
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
langgraph-checkpoint-postgres==3.1.2

# Cross-cutting
httpx==0.28.1
slowapi==0.1.10
